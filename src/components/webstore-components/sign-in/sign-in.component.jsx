import {
  signInWithGooglePopup,
  createUserDocument,
} from '../../../utils/firebase/firebase.utils';

const SignIn = () => {
  const handleChange = (event) => {
    const { name, value } = event.target;
  };

  return <div className="sign-in-container"></div>;
};

export default SignIn;
